const heartToggle = () => {
  const heart = document.querySelector('.index-heart-toggle')
  heart.addEventListener('click', (event) => {
    // event.currentTarget.toggle(Pin.create(user_id: 58, collection_id: 44)
    // event.currentTarget.toggle(Pin.destroy)
  console.log('here');
  });
};

  export { heartToggle }
